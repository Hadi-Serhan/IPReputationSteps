name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests with coverage (>= 90%)
        run: |
          pytest -q \
            --cov=check_ip \
            --cov=check_ip_batch \
            --cov=common \
            --cov-report=term-missing \
            --cov-fail-under=90
  docker:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Build check-ip image
        run: docker build --target check-ip -t check-ip .

      - name: Build check-ip-batch image
        run: docker build --target check-ip-batch -t check-ip-batch .

      - name: Smoke test check-ip (no key)
        run: |
          out=$(docker run --rm -e IP_ADDRESS="8.8.8.8" check-ip)
          echo "$out" | jq .

      - name: Smoke test check-ip-batch (no key)
        run: |
          out=$(docker run --rm -e IP_ADDRESSES="8.8.8.8,invalid-ip" check-ip-batch)
          echo "$out" | jq .